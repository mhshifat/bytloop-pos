/**
 * Public re-export point for the generated API client.
 *
 * The `generated/` directory is populated by `pnpm generate:api`. Prefer
 * importing from this index (`@/lib/api-client`) over deep paths.
 */

// Re-exports will be added as the generator runs.
export * from "./client-config";
